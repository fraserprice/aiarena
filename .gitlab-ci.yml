stages:
    - build
    - deploy

build:
    stage: build
    script:
        - npm run install-client
    artifacts:
        paths:
            - client/node_modules

deploy:
    stage: deploy
    script:
        - npm run start-client
