stages:
    - build
    - deploy

build:
    stage: build
    script:
        - npm run install-client
    artifacts:
        paths:
            - client/node_modules

deploy:
    stage: deploy
    scrip:
        - npm run start-client
