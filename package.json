{
  "name": "webapps_11",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-app": "cd app && npm install",
    "install-client": "cd client && npm install",
    "install": "npm run install-app; npm run install-client",
    "start-app": "cd app && npm start",
    "start-client": "cd client && npm start",
    "start": "concurrently --kill-others \"npm run start-app\" \"npm run start-client\""
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.doc.ic.ac.uk:lab1617_summer/webapps_11.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.4.0"
  }
}
